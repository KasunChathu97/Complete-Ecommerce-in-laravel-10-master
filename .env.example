APP_NAME="DL"
APP_ENV=local
APP_KEY=base64:Tjjy4TfdarBd26GuhQPxo/HuDrivbxeflY/tUbBNj50=
APP_DEBUG=true

LOG_CHANNEL=stack

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE="Your DB name"
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=null
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# --- SMS (Twilio) ---
# When enabled, every new row inserted into sms_logs will trigger:
# - Email notifications (admin + customer)
# - SMS notifications via Twilio (customer phone + admin phone from settings)
SMS_ENABLED=false
SMS_PROVIDER=twilio

# Optional: if your app stores phone numbers without a leading '+', set this so
# numbers like 0777820662 or 94777820662 can be normalized to E.164.
# Example for Sri Lanka: +94
SMS_DEFAULT_COUNTRY_CODE=

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
